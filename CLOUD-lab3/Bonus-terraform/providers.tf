terraform {
  required_version = ">=1.0"

  required_providers {
    azapi = {
      source  = "azure/azapi"
      version = "~>1.5"
    }
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~>3.0"
    }
  }
}

provider "azurerm" {
  features {}

  # TODO: add your service principal credentials.
  subscription_id = 
  tenant_id       = 
  client_id       = 
  client_secret   = 
}
